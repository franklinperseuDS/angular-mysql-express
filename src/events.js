const express = require('express');

function createRouter(db) {
    const router = express.Router();
    const owner = "";


    //the router are defined here

    return router;
}

module.exports = createRouter;



router.post('/event', (req, res, next) => {